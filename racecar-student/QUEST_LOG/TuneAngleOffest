import sys

sys.path.insert(1, '../../library')
import racecar_core


rc = racecar_core.create_racecar()

global offset
offset = 0.0
def update(): 
    global offset
    rc.drive.set_speed_angle(0.2, offset)

def update_slow():
    global offset
    print(offset)



if __name__ == "__main__":
    rc.set_start_update(start, update, update_slow)
    rc.go()